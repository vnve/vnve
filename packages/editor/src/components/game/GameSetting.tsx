import { Card, CardContent } from "@/components/ui/card";

export function GameSetting() {
  return (
    <Card className="flex-1 rounded-md relative">
      <CardContent className="relative h-full p-2"></CardContent>
    </Card>
  );
}
